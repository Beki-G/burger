<li>
    {{id}}. {{burger_name}}
    {{#if devoured}}
        <button class="change-devour bg-pink-500 hover:bg-pink-700 text-white py-2 px-4 rounded-full" data-id="{{id}}" data-devour="{{devoured}}">Devour!</button>
    {{/if}}
</li>